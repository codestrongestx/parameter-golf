# Scripts

This folder is for reusable operational helpers.

Current contents:

- `runpod_jupyter_exec.py`: Runpod Jupyter HTTPS fallback for executing shell
  commands, fetching files, and uploading files when SSH from a sandbox is
  unreliable.

Campaign-specific helpers should stay with their campaign unless they become
generally reusable.
