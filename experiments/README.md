# Experiments Index

Experiments are split by role, not by date.

## Active

`experiments/active/` contains live directions.

Keep one idea per folder.

Start here when resuming work:

- `experiments/active/README.md`
- `experiments/benchmarks/leaderboard_1xh100_replay/STATUS.md`

## Benchmarks

`experiments/benchmarks/` contains cross-cutting replay and measurement work.

Current benchmark harness:

- `experiments/benchmarks/leaderboard_1xh100_replay/`

## Archive

`experiments/archive/` contains older experiment trees that are still useful for
reference but should not be mistaken for the current mainline.

Current archived trees:

- `experiments/archive/grant_2026_q1/`
- `experiments/archive/public_stack_pivot/`
- `experiments/archive/depth_film_recurrent/`
