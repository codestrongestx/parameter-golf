# Active Experiments

`experiments/active/` is for live work only.

Rules:

- one idea per folder
- no bundled multi-lane campaign folders
- if a direction stops driving decisions, move it to `experiments/archive/`

Current state:

- no new active direction folder has been promoted yet
- use `experiments/benchmarks/leaderboard_1xh100_replay/STATUS.md` as the
  current source of truth for what should be built next
